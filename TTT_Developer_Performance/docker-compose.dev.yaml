version: '3.8'

services:
  apache-php:
    build: .
    container_name: laravel-osssd-${CLUSTER_NAME}-TTTDP
    ports:
      - "1300:80"
    volumes:
      - .:/var/www/html
      # - /var/www/html/vendor
    env_file:
      - .env